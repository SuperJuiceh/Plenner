<UserControl x:ConnectionId='1'
    x:Class="Planner.Modules.TimeFlowPanel"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Planner.Modules"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:converters="using:Planner.Data.Converters"
    xmlns:planning="using:DataLab.Data.Planning"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">
    <UserControl.Resources>
        <converters:TimeToElementWidthConverter x:ConnectionId='2' x:Key="timeToWidthConv" x:Name="timeToWidthConv"/>
        <converters:PlanningItemToInfoPanelConverter x:ConnectionId='3' x:Key="itemToPanelInfo" x:Name="itemToPanelInfo"/>
        <converters:ItemTimeToMarginConverter x:ConnectionId='4' x:Key="timeToMarginConv" x:Name="timeToMarginConv"/>
    </UserControl.Resources>
    <RelativePanel x:ConnectionId='5' x:Name="MainPanel"                                 Background="White" HorizontalAlignment="Left" VerticalAlignment="Top" Height="420">
        <!--<RelativePanel.Resources>
                <Style x:Key="horizontalListBoxStyle" TargetType="ItemsControl">
                    <Setter Property="ItemsPanel">
                        <Setter.Value>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Top" HorizontalAlignment="Left" Width="16.6"/>
                            </ItemsPanelTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </RelativePanel.Resources>-->

        <Grid x:ConnectionId='6' x:Name="numbersTableHeaderPanel"                                 HorizontalAlignment="Left" VerticalAlignment="Top"/>
        <Button x:ConnectionId='7' x:Name="configFlyoutOpenButton" Width="65" Height="39"                                      >
            <Button.Flyout>
                <Flyout>
                    <StackPanel Orientation="Vertical">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Enabled Items"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <CheckBox x:ConnectionId='24' Content="Activities" HorizontalAlignment="Left" Margin="10,15,0,0" VerticalAlignment="Top"                                     IsChecked="{Binding ActivitiesEnabled, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                            <CheckBox x:ConnectionId='25' Content="Reflections" HorizontalAlignment="Left" Margin="10,15,0,0" VerticalAlignment="Top"                                     IsChecked="{Binding ReflectionsEnabled, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                            <CheckBox x:ConnectionId='26' Content="ToDoItems" HorizontalAlignment="Left" Margin="10,15,0,0" VerticalAlignment="Top"                                     IsChecked="{Binding TodoitemsEnabled, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Date"/>
                            <DatePicker x:ConnectionId='23' Margin="40, 0, 0, 0" x:Name="timeFlowDatePicker"                                             />
                        </StackPanel>
                    </StackPanel>
                </Flyout>
            </Button.Flyout>
            <Button.Background>
                <ImageBrush ImageSource="/Assets/settings_icon.png" />
            </Button.Background>
        </Button>
        <RelativePanel x:ConnectionId='8' Height="380" Margin="0,60,0,0" HorizontalAlignment="Left" VerticalAlignment="Top"                                 Background="Black">
            <RelativePanel x:ConnectionId='9' Height="380" Margin="0,60,0,0"                                 HorizontalAlignment="Left" >
                <RelativePanel x:ConnectionId='10' Height="380" Margin="0,0,0,0"                                 HorizontalAlignment="Left"  >
                    <ItemsControl x:ConnectionId='19' x:Name="timeLineActivitiesItemsControl"                                                        Height="127"                                 HorizontalAlignment="Left" >
                        <ItemsControl.ItemTemplate>
                            <DataTemplate                                   >
                                <RelativePanel x:ConnectionId='20' Height="126" Width="400" HorizontalAlignment="Left" >
                                    <RelativePanel x:ConnectionId='21' Background="Cyan" x:Name="activitiesPanel"                                                               Height="126"                                                                                                                      >
                                        <TextBlock x:ConnectionId='22' Height="126" ToolTipService.ToolTip="{Binding Converter={StaticResource itemToPanelInfo}}"                                                                                                                       />
                                    </RelativePanel>
                                </RelativePanel>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </RelativePanel>
                <RelativePanel x:ConnectionId='11' Height="255" Margin="0,127,0,0"                                 HorizontalAlignment="Left" >
                    <ItemsControl x:ConnectionId='16'                                                         Height="256"                                 HorizontalAlignment="Left" >
                        <ItemsControl.ItemTemplate>
                            <DataTemplate                                   >
                            <RelativePanel x:ConnectionId='17' x:Name="reflectionsTextBlock" Background="Green"                                                               HorizontalAlignment="Left"  Height="126"                                                                                                                           >
                                    <TextBlock x:ConnectionId='18' x:Name="lol1" Height="126" ToolTipService.ToolTip="{Binding Converter={StaticResource itemToPanelInfo}}"                                                                                                                             >
                                    </TextBlock>
                                </RelativePanel>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </RelativePanel>
                <RelativePanel x:ConnectionId='12' Height="127" Margin="0,255,0,0"                                 HorizontalAlignment="Left" >
                    <ItemsControl x:ConnectionId='13'                                                       Height="127"                                 HorizontalAlignment="Left" >
                        <ItemsControl.ItemTemplate>
                            <DataTemplate                                   >
                            <RelativePanel x:ConnectionId='14' x:Name="todoitemsPanel" Background="Red" Height="126"                                                               HorizontalAlignment="Left"                                                                                                                     >
                                    <TextBlock x:ConnectionId='15' Height="126" ToolTipService.ToolTip="{Binding Converter={StaticResource itemToPanelInfo}}"                                                                                                                     >

                                    </TextBlock>
                                </RelativePanel>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </RelativePanel>
            </RelativePanel>
        </RelativePanel>
    </RelativePanel>
</UserControl>

