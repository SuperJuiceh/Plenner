<Page x:ConnectionId='1'
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Planner"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:conv="using:Planner.Data.Converters" xmlns:Maps="using:Windows.UI.Xaml.Controls.Maps"
    xmlns:modules="using:Planner.Modules"
    x:Class="Planner.ActivitiesPage"
    mc:Ignorable="d"
    Width="350" Height="635"
    >
    
    <Page.Resources>
        <conv:DateTimeFormatConverter x:Key="DTFormatConv" />
        <conv:RandomColorGenerator  x:Key="RandomColor" />
        <conv:DaysListToStringConverter x:Key="daysToStringConv" />
        <conv:BoolToElementVisibility x:Key="boolToVisibilityConverter"/>

    </Page.Resources>
    <SplitView x:ConnectionId='2' x:Name="mainSplitView" DisplayMode="CompactOverlay" OpenPaneLength="50" CompactPaneLength="20"                                  >
        <StackPanel Orientation="Vertical">

            <StackPanel Orientation="Horizontal" Height="39">
                <Button HorizontalAlignment="Left" Margin="200,0,0,0" VerticalAlignment="Top" Height="31" Width="41">
                    <Button.Flyout>
                        <Flyout>
                            <StackPanel Orientation="Vertical">
                                <TextBlock Text="Enabled Modules"/>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="Single Activities"/>
                                    <CheckBox x:ConnectionId='24' x:Name="showActivitiesSingleCheckbox"                                          IsChecked="{Binding ActivitiesSingleListVisible, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"                                               />
                                </StackPanel>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="Set Activities"/>
                                    <CheckBox x:ConnectionId='23' x:Name="showRepeatingActivitiesCheckbox"                                           IsChecked="{Binding ActivitiesRepeatingListVisible, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"                                                  />

                                </StackPanel>
                            </StackPanel>
                        </Flyout>
                    </Button.Flyout>
                    <Button.Background>
                        <ImageBrush ImageSource="/Assets/eye.png"/>
                    </Button.Background>
                </Button>
                <Button x:ConnectionId='21' x:Name="button" HorizontalAlignment="Left" Margin="0,0,0,0" VerticalAlignment="Top" Height="32" Width="36"                        >
                    <Button.Background>
                        <ImageBrush ImageSource="/Assets/plus.png" />
                    </Button.Background>
                </Button>
                <Button x:ConnectionId='22' x:Name="button1" HorizontalAlignment="Left" Margin="0,0,0,0" VerticalAlignment="Top" Height="35" Width="42"                       >
                    <Button.Background>
                        <ImageBrush ImageSource="/Assets/Broom_icon.svg.png" />
                    </Button.Background>
                </Button>
            </StackPanel>
            <TextBlock x:ConnectionId='3' x:Name="textBlock" HorizontalAlignment="Left" Margin="0,15,0,0" TextWrapping="Wrap" Text="Activities" FontSize="20" VerticalAlignment="Top" Height="30" Width="115"                                              />

            <ListView x:ConnectionId='4' x:Name="listBoxSingleItems"                                          FontSize="10" HorizontalAlignment="Left" Height="246" Margin="0,0,0,0" VerticalAlignment="Top" Width="314"                                        ScrollViewer.VerticalScrollBarVisibility="Auto" MaxHeight="160" Visibility="{Binding ActivitiesSingleListVisible, Converter={StaticResource boolToVisibilityConverter}, UpdateSourceTrigger=PropertyChanged}"  >
                <ListView.Header>
                    <RelativePanel Height="50">
                        <StackPanel Orientation="Horizontal" Margin="0, 10, 0, 0">
                            <Button Margin="250,-2,0,0.333" Width="24" Height="25" >
                                <Button.Background>
                                    <ImageBrush ImageSource="/Assets/FilterIcon.png"/>
                                </Button.Background>
                                <Button.Flyout> 
                                    <Flyout x:ConnectionId='11' x:Name="filterButton"                             >
                                        <StackPanel Margin="0, 20, 0, 0" Orientation="Vertical" Width="351" Height="186">

                                            <TextBlock Text="Filter Options"/>
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock x:ConnectionId='15' x:Name="filterQueryTextBlock" Text="Search key" />
                                                <TextBox Margin="30, 0, 0, 0" Text="..." Width="228"/>
                                            </StackPanel>
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="From"/>
                                                <DatePicker x:ConnectionId='14' x:Name="filterStartDate" Margin="20,0,0,0" />
                                            </StackPanel>
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="To"/>
                                                <DatePicker x:ConnectionId='13' x:Name="filterEndDate" Margin="20,0,0,0" />
                                            </StackPanel>
                                            <Button x:ConnectionId='12' x:Name="filterOkButton" Content="OK"                             />
                                        </StackPanel>
                                    </Flyout>
                                </Button.Flyout>
                            </Button>
                        </StackPanel>

                        <TextBlock x:ConnectionId='9' x:Name="singleActivityNameTextBlock" Text="Name" Margin="0, 20, 0, 0"                                            />
                        <TextBlock x:ConnectionId='10' x:Name="singleActivityTimeTextBlock"  Text="Time" Margin="100, 20, 0, 0"                                            />
                        <TextBlock Text="" Margin="170, 20, 0, 0" />
                        <TextBlock Text="" Margin="220, 20, 0, 0" />
                        <TextBlock Text="" Margin="250, 20, 0, 0" />
                        <TextBlock Text="" Margin="280, 20, 0, 0" />
                    </RelativePanel>
                </ListView.Header>
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <RelativePanel x:ConnectionId='16' x:Name="relativePanel" Margin="-10, 10, 0, 0" Height="35"                                                   >
                            <TextBlock Text="{Binding Name}"  FontSize="10" />
                            <TextBlock Text="{Binding Start, Converter={StaticResource DTFormatConv}, ConverterParameter={Binding End}}" Margin="100, 0, 0, 0" FontSize="10" />

                            <Button x:Name="button2" HorizontalAlignment="Left" Margin="170, 0, 0, 0" VerticalAlignment="Top" Height="25" Width="25" RenderTransformOrigin="-5.786,-0.905" FontSize="10" >
                                <Button.Background>
                                    <ImageBrush ImageSource="/Assets/GPS.png" />
                                </Button.Background>
                                <Button.Flyout>
                                    <Flyout>
                                        <RelativePanel Height="396">
                                            <local:PlannerMapControl x:ConnectionId='20' x:Name="name" Mode="OBSERVANT_VIEW"  DataContext="{Binding }" MovementMode="FOOT"                     />
                                        </RelativePanel>
                                    </Flyout>
                                </Button.Flyout>
                            </Button>
                            <Button x:ConnectionId='17' x:Name="editButton"                          Height="25" Width="25" FontSize="10" Margin="220, 0, 0, 0">
                                <Button.Background>
                                    <ImageBrush ImageSource="/Assets/edit.png" />
                                </Button.Background>
                            </Button>
                            <Button x:ConnectionId='18' x:Name="mailActivityButton" Height="25" Width="25" FontSize="10" Margin="250, 0, 0, 0"                                  Tag="{Binding }" >
                                <Button.Background>
                                    <ImageBrush ImageSource="/Assets/mail.png" />
                                </Button.Background>
                            </Button>
                            <Button x:ConnectionId='19'                      Height="25" Width="25" FontSize="10" Margin="280, 0, 0, 0" >
                                <Button.Background>
                                    <ImageBrush ImageSource="/Assets/delete.png" />
                                </Button.Background>
                            </Button>
                            <TextBlock x:Name="descriptionTextBlock" Text="{Binding Description}" Visibility="Collapsed" Margin="0,20,0,0" />
                        </RelativePanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

            <ListView x:ConnectionId='5' x:Name="listView"                                            HorizontalAlignment="Left" Height="205" Margin="0,100,0,0" VerticalAlignment="Top" Width="314"                                          FontSize="10" ScrollViewer.VerticalScrollBarVisibility="Auto" MaxHeight="180" Visibility="{Binding ActivitiesRepeatingListVisible, Converter={StaticResource boolToVisibilityConverter}, UpdateSourceTrigger=PropertyChanged}" >
                <ListView.Header>
                    <RelativePanel Height="100">
                        <TextBlock Text="Repeating" HorizontalAlignment="Left" Margin="0,0,0,0" TextWrapping="Wrap" VerticalAlignment="Top" Width="41"/>

                        <TextBlock x:ConnectionId='6' x:Name="repeatingNameHeader" Text="Name"                                     Margin="0, 20, 0, 0" />
                        <TextBlock Text="Days" Margin="100, 20, 0, 0"/>
                        <TextBlock x:ConnectionId='7' x:Name="repeatingTimeHeader" Text="Time"                                     Margin="200, 20, 0, 0"/>
                        <TextBlock Text="..." Margin="250, 20, 0, 0" />
                    </RelativePanel>
                </ListView.Header>
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <RelativePanel Margin="-10, 0, 0, 0">
                            <TextBlock Text="{Binding Name}" Margin="0, 0, 0, 0" FontSize="10" />
                            <TextBlock Text="{Binding Days, Converter={StaticResource daysToStringConv}  }" Margin="100, 0, 0, 0" FontSize="10" />
                            <TextBlock Text="{Binding Time, Converter={StaticResource DTFormatConv}}" Margin="200, 0, 0, 0" FontSize="10" />
                            <Button x:ConnectionId='8'                         Height="14" Width="14" FontSize="10" Margin="250, 0, 0, 0" >
                                <Button.Background>
                                    <ImageBrush ImageSource="/Assets/delete.png" />
                                </Button.Background>
                            </Button>
                        </RelativePanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

        </StackPanel>
        <SplitView.Pane>

            <modules:NavigationPanel ActivitiesButtonEnabled="False"/>
        </SplitView.Pane>
        <SplitView.Background>
            <ImageBrush ImageSource="/Assets/Background_cyanlight.png" />
        </SplitView.Background>

    </SplitView>
</Page>

